{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "okta.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/",
      "handler": "okta.index",
      "config": {
        "policies": []
      }
    }
  ]
}
